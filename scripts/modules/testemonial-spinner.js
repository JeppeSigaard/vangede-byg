if ($('.testemonial-spinner').length){
    
    var hero = $('.testemonial-spinner').flickity({
        prevNextButtons: false,
        cellSelector: '.testemonial',
        accessibility: true,
        wrapAround: true,
    });
    
}